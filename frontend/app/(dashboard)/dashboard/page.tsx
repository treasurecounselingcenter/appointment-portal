"use client";
import Link from "next/link";
import { FiCalendar, FiClock, FiUsers } from "react-icons/fi";

const appointments = [{ patient: "Amelia Carter", type: "General consultation", time: "09:30 AM", status: "Confirmed" }, { patient: "Noah Williams", type: "Follow-up visit", time: "11:00 AM", status: "Pending" }, { patient: "Olivia Brown", type: "Annual check-up", time: "01:30 PM", status: "Confirmed" }];
export default function DashboardPage() { return <><div className="page-heading"><div><h1>Dashboard</h1><p>Here is what is happening with your practice today.</p></div></div><div className="stats-grid"><div className="stat-card"><span>Today&apos;s appointments</span><strong><FiCalendar />12</strong></div><div className="stat-card"><span>Active clients</span><strong><FiUsers />248</strong></div><div className="stat-card"><span>Hours scheduled</span><strong><FiClock />6.5 <small>hrs</small></strong></div></div><section className="content-card"><div className="card-heading"><h3>Latest appointments</h3><Link href="/appointments">View all</Link></div><AppointmentTable rows={appointments} /></section></>; }
export function AppointmentTable({ rows }: { rows: { patient: string; type: string; time: string; status: string }[] }) { return <div className="table-scroll"><table><thead><tr><th>Client</th><th>Appointment</th><th>Time</th><th>Status</th></tr></thead><tbody>{rows.map((row) => <tr key={row.patient}><td>{row.patient}</td><td>{row.type}</td><td>{row.time}</td><td><span className={`status ${row.status.toLowerCase()}`}>{row.status}</span></td></tr>)}</tbody></table></div>; }
